import { Activity, CalendarClock, Eye, Leaf, PlugZap, Zap } from "lucide-react";
import type { AppLocale } from "@/lib/i18n";

const EXAMPLES = [
  { name: "Parque Solar Horizonte", capacity: "180 MW", connection: 70, connectionStatus: { es: "Informe de autorización de conexión final", en: "Final connection authorization report" }, environmental: 55, environmentalStatus: { es: "En calificación", en: "Under assessment" }, movement: { es: "Fecha de conexión actualizada", en: "Connection date updated" }, date: "03-08-2026" },
  { name: "BESS Cordillera Central", capacity: "120 MW / 480 MWh", connection: 82, connectionStatus: { es: "Autorizado para declararse en construcción", en: "Authorized to be declared under construction" }, environmental: 100, environmentalStatus: { es: "Aprobado", en: "Approved" }, movement: { es: "Nuevo hito ambiental", en: "New environmental milestone" }, date: "02-08-2026" },
];

export function TrackingPreview({ locale }: { locale: AppLocale }) {
  const en = locale === "en";
  return <div className="space-y-4"><div className="grid grid-cols-3 gap-3">{[{ label: en ? "Followed projects" : "Proyectos seguidos", value: "12" }, { label: en ? "Recent changes" : "Movimientos recientes", value: "7" }, { label: en ? "Projects with updates" : "Proyectos con novedades", value: "4" }].map((item) => <div key={item.label} className="rounded-lg bg-neutral-50 p-3 dark:bg-neutral-900"><p className="text-[10px] text-neutral-500">{item.label}</p><p className="mt-1 text-xl font-semibold text-neutral-950 dark:text-white">{item.value}</p></div>)}</div><div className="grid gap-3 lg:grid-cols-2">{EXAMPLES.map((project) => <article key={project.name} className="rounded-lg border border-neutral-200 bg-white p-4 shadow-sm dark:border-neutral-800 dark:bg-neutral-950"><div className="flex items-start justify-between gap-3"><div><p className="font-semibold text-neutral-950 dark:text-white">{project.name}</p><p className="mt-1 text-xs text-neutral-500">{en ? "Following since July 2026" : "Siguiendo desde julio de 2026"}</p></div><div className="flex items-center gap-2"><span className="rounded-md bg-brand-surface px-2 py-1 text-xs font-semibold text-brand-deep"><Zap size={12} className="mr-1 inline" />{project.capacity}</span><Eye size={18} className="text-brand-deep" /></div></div><div className="mt-4 grid gap-3 sm:grid-cols-2"><div className="rounded-lg bg-neutral-50 p-3 dark:bg-neutral-900"><p className="flex items-center gap-1.5 text-xs text-neutral-500"><PlugZap size={13} />{en ? "Connection status" : "Estado de conexión"}</p><div className="mt-2 h-2.5 overflow-hidden rounded-full bg-brand-primary/15"><div className="h-full rounded-full bg-brand-primary" style={{ width: `${project.connection}%` }} /></div><p className="mt-1 text-xs font-semibold text-neutral-800 dark:text-neutral-100">{project.connection}%</p><p className="mt-1 text-[11px] leading-4 text-neutral-500">{en ? project.connectionStatus.en : project.connectionStatus.es}</p></div><div className="rounded-lg bg-neutral-50 p-3 dark:bg-neutral-900"><p className="flex items-center gap-1.5 text-xs text-neutral-500"><Leaf size={13} />{en ? "Environmental status" : "Estado ambiental"}</p><div className="mt-2 h-2.5 overflow-hidden rounded-full bg-brand-primary/15"><div className="h-full rounded-full bg-brand-primary" style={{ width: `${project.environmental}%` }} /></div><p className="mt-1 text-xs font-semibold text-neutral-800 dark:text-neutral-100">{project.environmental}%</p><p className="mt-1 text-[11px] leading-4 text-neutral-500">{en ? project.environmentalStatus.en : project.environmentalStatus.es}</p></div></div><div className="mt-3 flex gap-2 border-t border-neutral-100 pt-3 text-xs dark:border-neutral-800"><CalendarClock size={14} className="shrink-0 text-neutral-400" /><p><span className="font-semibold">{en ? "Latest change:" : "Último movimiento:"}</span> {en ? project.movement.en : project.movement.es} · {project.date}</p></div></article>)}</div><p className="flex gap-2 text-[11px] text-neutral-400"><Activity size={13} />{en ? "Fictional data representative of the Prime tracking view." : "Datos ficticios representativos de la vista Prime de Seguimiento."}</p></div>;
}
