import type { Metadata } from "next";
import MatrizPage from "../mercado/page";

export const metadata: Metadata = { title: "Proyectos en Operación — Transition LATAM" };
export const dynamic = "force-dynamic";

export default MatrizPage;
